export default function Header() {
  return (
    <header style={{padding: '20px', backgroundColor: '#f5f5f5', textAlign: 'center'}}>
      <h1>Golden Quill</h1>
    </header>
  );
}






// --- IGNORE ---


// basic structure
// 1. bank - all prevous prompts - laynie

// 2. intake form save edit update all of there stuff - christian 

// 3. ai space - Rene

// 4. save button
// 5. ai button - part of the component display

// further breaking this down
// all of this stuff is going to be wrap in a main app component 
// basically a big <div> </div> that holds every other component
// within that main app component we have 4 main components
// 1. title header component
// 2. bank component 
// 3. intake form component
// 4. ai space component
// 5. extra buttons component (save button and ai button)

// each of those components will have their own folder within the components folder
// each component folder will have an index.tsx file that holds the main code for that component
// each component folder will also have a styles.css file that holds the styles for that component

// each component will be imported into the main app component and rendered within the main app component's return statement
// t

// --- IGNORE --